{
  "name": "adhd-daily-planner-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check src/callables/premiumAi.js && node --check src/callables/premiumStatus.js && node --check src/openai/client.js && node --check src/openai/prompts.js && node --check src/openai/validation.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "openai": "^4.104.0"
  }
}
